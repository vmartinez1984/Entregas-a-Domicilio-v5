<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EntregaADomicilio.Pedidos.Maui.Paginas.PaginaDeCategoria"
             Title="{Binding Nombre}">
    <VerticalStackLayout>
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <ActivityIndicator x:Name="ActivityIndicator"/>
        <ListView x:Name="ListView" ItemTapped="ListView_ItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="2*"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <Label Text="{Binding Nombre}"/>
                            <Label Text="{Binding Descripcion}" Grid.Row="1"/>
                            <Label Text="{Binding Precio, StringFormat='{}{0:C2}'}" Grid.Row="2"/>

                            <Image Source="{Binding EnlaceDeImagen}" Grid.Column="1" Grid.Row="1" WidthRequest="150"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </VerticalStackLayout>
</ContentPage>